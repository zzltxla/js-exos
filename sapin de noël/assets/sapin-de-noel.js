let input = document.querySelector("input");
let btn = documnet.querySelector("button");
let body = document.querySelector("body");
let usr = input.value;
//VAR REGION ENDS HERE 

function tree() {
    if (usr === (/[0-9]/)) {
        for (let i = 0; i >= 0; i++) {
            
            
        }
    } else {
        alert("Please eneter a valid number");
    }
}
btn.addEventListener('click', tree);